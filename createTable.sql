CREATE EXTERNAL TABLE IF NOT EXISTS l1x_logs.dev (
  `DateTime` STRING,
  `EdgeLocation` STRING,
  `ScBytes` INTEGER,
  `CIp` STRING,
  `CsMethod` STRING,
  `CsHost` STRING,
  `CsUriStem` STRING,
  `ScStatus` STRING,
  `CsReferer` STRING,
  `CsUserAgent` STRING,
  `CsUriQuery` STRING,
  `CsCookie` STRING,
  `XEdgeResultType` STRING,
  `XEdgeRequestId` STRING,
  `XHostHeader` STRING,
  `CsProtocol` STRING,
  `CsBytes` INTEGER,
  `TimeTaken` STRING,
  `XForwardedFor` STRING,
  `SslProtocol` STRING,
  `SslCipher` STRING,
  `XEdgeResponseResultType` STRING,
  `CsProtocolVersion` STRING,
  `FleStatus` STRING,
  `FleEncryptedFields` STRING,
  `CPort` STRING,
  `TimeToFirstByte` STRING,
  `XEdgeDetailedResultType` STRING,
  `ScContentType` STRING,
  `ScContentLen` INTEGER,
  `ScRangeStart` STRING,
  `ScRangeEnd` STRING
) PARTITIONED BY (`month` string) STORED AS PARQUET LOCATION 's3://logs.l1x.be/dwh/' -- MSCK REPAIR TABLE l1x_logs.dev;
